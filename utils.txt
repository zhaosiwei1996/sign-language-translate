# 获取当前时间戳
timestamp = MyUtils.get_timestamp()
print(timestamp)

# 将 JSON 转换为字符串
data = {'name': 'John', 'age': 30}
json_string = MyUtils.json_to_string(data)
print(json_string)

# 将字符串转换为 JSON
json_data = MyUtils.string_to_json(json_string)
print(json_data)

# 读取文件内容
file_content = MyUtils.read_file('file.txt')
print(file_content)

# 写入文件内容
MyUtils.write_file('file.txt', 'Hello, World!')

# 追加内容到文件
MyUtils.append_to_file('file.txt', 'Append text')

# 获取当前系统类型
system_type = MyUtils.get_system_type()
print(system_type)

# 发送 POST 请求
url = 'https://example.com/api'
data = {'name': 'John', 'age': 30}
response = MyUtils.send_post_request(url, data)
print(response)

# 发送 GET 请求
url = 'https://example.com/api'
params = {'page': 1, 'limit': 10}
response = MyUtils.send_get_request(url, params)
print(response)

# 发送 DELETE 请求
url = 'https://example.com/api/1'
response = MyUtils.send_delete_request(url)
print(response)

# 发送 PUT 请求
url = 'https://example.com/api/1'
data = {'name': 'John', 'age': 30}
response = MyUtils.send_put_request(url, data)
print(response)

# 执行系统命令
command = 'ls -l'
result = MyUtils.execute_command(command)
print(result)